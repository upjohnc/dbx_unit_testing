{
    "creator_user_name": "{{ creator }}",
    "clusters": [
        {
            "label": "default",
            "autoscale": {
                "min_workers": 1,
                "max_workers": 1
            }
        }
    ],
    "development": true,
    "continuous": false,
    "channel": "CURRENT",
    "edition": "advanced",
    "libraries": [
        {
            "notebook": {
                "path": "/Repos/{{ repo_name }}/test/test_data_setup"
            }
        },
        {
            "notebook": {
                "path": "/Repos/{{ repo_name }}/pipeline_code/invoice_processing"
            }
        },
        {
            "notebook": {
                "path": "/Repos/{{ repo_name }}/pipeline_code/invoice_processing_expectations"
            }
        }
    ],
    "name": "{{ pipeline_name }}",
    "photon": false,
    "storage": "{{ storage }}",
    "target": "{{ target }}"
}
