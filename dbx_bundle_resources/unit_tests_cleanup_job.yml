resources:
  jobs:
    unit_test_cleanup_job:
      name: unit_test_cleanup_job

      tasks:
        - task_key: refresh_pipeline
          notebook_task:
            notebook_path: ../clean_up.py
          new_cluster:
            spark_version: 13.3.x-scala2.12
            node_type_id: i3.xlarge
            num_workers: 1
